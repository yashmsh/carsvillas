pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Add commands to build your project
                sh 'echo "Building project..."'
                sh 'npm install' // Example: If you're using Node.js
                sh 'npm run build' // Example: Build step for Node.js project
            }
        }
        // Other stages like Test and Deploy would go here
    }

    // Post-build actions would go here
}
